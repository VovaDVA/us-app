services:
  db:
    image: postgres:15
    ports:
      - "5434:5432"   # вместо 5432 используем 5433 на хосте
    environment:
      POSTGRES_USER: dva
      POSTGRES_PASSWORD: dva
      POSTGRES_DB: us
    volumes:
      - db_data:/var/lib/postgresql/data


  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: us_server
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/us
      - SPRING_DATASOURCE_USERNAME: dva
      - SPRING_DATASOURCE_PASSWORD: dva
      - SPRING_DATASOURCE_DRIVER-CLASS-NAME: org.postgresql.Driver
      - SPRING_PROFILES_ACTIVE=dev
    depends_on:
      - db
    healthcheck:  # Проверка здоровья БД
      test: ["CMD", "pg_isready", "-h", "db", "-U", "dva"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - diary_uploads:/app/uploads/diary

volumes:
  db_data:
  diary_uploads:

networks:
  default:
    driver: bridge
    ipam:
      config:
        - subnet: 172.25.0.0/16
